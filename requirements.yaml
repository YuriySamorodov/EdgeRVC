name: edgervc
channels:
  - conda-forge
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pip<24.1
  # Runtime/system libs commonly needed by audio Python packages.
  - ffmpeg
  - libsndfile
  - pip:
      - -r requirements.txt
